# --- Node / app (added in Phase 2) ---
node_modules/
dist/
build/
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- Env / secrets (only *.example is tracked) ---
.env
.env.local
.env.*.local

# --- Supabase CLI local state ---
supabase/.branches/
supabase/.temp/
supabase/functions/.env

# --- Self-hosted stack: real config & data (see *.example) ---
infrastructure/**/.env
infrastructure/**/volumes/
# The real infrastructure/supabase/volumes/api/kong.yml is generated by copying
# kong.yml.example and filling in the dashboard credentials — never commit it.
